# PostgreSQL 连接
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=vocabquest

# JWT 签名密钥（生产环境务必修改）
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d

# 服务端口
PORT=3001

# Gemini AI（可选，未配置时 AI 生成接口返回 503）
GEMINI_API_KEY=
